<div class="container">
  <h1 class="text-center my-4 display-4">Bem-vindo ao meu portfólio!</h1>
  <p class="text-center p-apresentacao">Meu nome é <span class="text-white">Kaique Costa</span> e sou desenvolvedor web fullstack</p>
  <p class="text-center p-apresentacao">Veja alguns dos meus projetos</p>
  <div class="row">
    <% @projects.each do |project| %>
      <div class="col-sm-12 col-md-6 col-lg-6 mb-4 d-flex">
        <div class="card h-100 rounded-lg custom-card custom-border">
          <% if project.image.present? %>
            <%= image_tag asset_path(project.image), class: 'card-img-top img-fluid', alt: project.title %>
          <% end %>
          <div class="card-body">
            <h4 class="card-title text-white custom-title"><%= project.title %></h4>
            <p class="card-text"><%= project.description %></p>
          </div>
          <div class="card-footer d-flex justify-content-between align-items-center">
            <a href="<%= project.link %>" class="btn btn-custom" target="_blank">Ver projeto</a>
             </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
